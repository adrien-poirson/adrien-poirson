import React from "react";
import Layout from "../components/layout/Layout";

const AboutPage = () => {
    return (
        <Layout>
            <h1>About Me</h1>
            {/* Add your bio, skills, experience, etc. */}
        </Layout>
    );
};

export default AboutPage;

export const Head = () => <title>About</title>;
